def add(x,y) -> float:
    return float(x + y)

def multiply(x, y) -> float:
    return float(x * y)

def square(x) -> float:
    return float(x * x)

def add_squares(x, y) -> float:
    return float(x * x + y * y)

